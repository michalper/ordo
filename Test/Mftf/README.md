# MFTF tests — scaffold

This directory follows the standard Magento MFTF layout (`Test/Mftf/Test/`, and eventually `ActionGroup/`, `Data/`, `Page/`, `Section/` as needed) per the [Adobe MFTF getting-started guide](https://developer.adobe.com/commerce/testing/functional-testing-framework/getting-started).

No `.xml` test cases exist yet — see README.md → Roadmap → Phase 6 for the planned first scenarios (order approval email round-trip, offer self-extension, credit limit checkout behavior).
