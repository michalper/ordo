<?xml version="1.0" encoding="UTF-8"?>
<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminRecencyPercentileConditionTest">
        <annotations>
            <features value="Ordo Automation"/>
            <stories value="RFM"/>
            <title value="A campaign's recency_percentile_at_least condition reads Cron\RecomputeRfmScores's precomputed table, not a live scan"/>
            <description value="Closes the last of §3's two RFM gaps in SCENARIOS.md - not just 'the condition works', but specifically that it reads the precomputed ordo_customer_rfm_score table RecomputeRfmScores maintains, rather than falling back to a live scan (RfmCalculator::readStoredPercentileRanks() only returns null - triggering the live fallback - when the table has never been populated at all; once it has ANY rows, a customer missing from it fails closed instead of falling back). RfmTestHelper seeds one throwaway 'poison' row first, so the table is already non-empty before the real customer's first order - if the condition read live instead of precomputed, it would already pass at that first dispatch with no cron involved at all. Only after CronScheduleHelper forces RecomputeRfmScores to run (real schedule 01:00 daily, see etc/crontab.xml) does a second real order's dispatch find the customer in the table and fire. percentile=0 is used deliberately (satisfied by any customer present in the ranks, no matter how they rank) - exact percentile values aren't the point of this test, being read from the right table at the right time is."/>
            <severity value="CRITICAL"/>
            <group value="ordo_automation"/>
            <group value="segment"/>
        </annotations>
        <before>
            <createData entity="Simple_US_Customer" stepKey="createCustomer"/>
            <createData entity="SimpleProduct2" stepKey="createProduct"/>
            <actionGroup ref="AdminLoginActionGroup" stepKey="loginAsAdmin"/>
            <!-- Makes ordo_customer_rfm_score non-empty before this test's own customer has
                 ever placed an order - see this test's own description for why that matters. -->
            <helper class="\Ordo\Automation\Test\Mftf\Helper\RfmTestHelper" method="insertPoisonRfmScoreRow" stepKey="insertPoisonRow"/>
        </before>
        <after>
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
            <deleteData createDataKey="createProduct" stepKey="deleteProduct"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logoutAsAdmin"/>
        </after>

        <!-- Step 1: the campaign - trigger=order_placed, condition=recency_percentile_at_least
             with the dedicated "percentile" field (added alongside this test - see
             ordo_campaign_form.xml's conditions switcherConfig - previously configurable only
             via the raw Params (JSON) fallback). -->
        <amOnPage url="admin/ordo/campaign/new" stepKey="goToNewCampaignPage"/>
        <waitForPageLoad stepKey="waitForNewCampaignPage"/>
        <waitForElementVisible selector="[name='name']" stepKey="waitForNameField"/>
        <fillField selector="[name='name']" userInput="Recency Percentile Send Email Campaign" stepKey="fillName"/>

        <waitForElementVisible selector="#ordo-campaign-flow" stepKey="waitForFlowCanvas"/>
        <executeJS function="window.ordoFlowTestHook.buildChain([
            {kind: 'trigger', type: 'order_placed'},
            {kind: 'condition', type: 'recency_percentile_at_least', fields: {params_json: '{&quot;percentile&quot;:0}'}},
            {kind: 'action', type: 'send_email', fields: {template: 'ordo_campaign_generic', message: 'Recency percentile condition fired.', delay_minutes: '0'}}
        ]);" stepKey="buildFlowGraph"/>

        <executeJS function="document.evaluate(&quot;//input[@name='enabled']/following-sibling::label&quot;, document, null, XPathResult.FIRST_ORDERED_NODE_TYPE, null).singleNodeValue.click();" stepKey="enableCampaign"/>
        <executeJS function="document.querySelector(&quot;[data-flow-action='apply']&quot;).click();" stepKey="clickApplyFlowAndSave"/>
        <waitForPageLoad stepKey="waitForSave"/>
        <see selector=".message-success" userInput="The campaign has been saved." stepKey="seeSuccessMessage"/>

        <!-- Step 2: a REAL first order - establishes real order history for RfmCalculator, but
             dispatches BEFORE the cron has ever run for this customer. With the table already
             non-empty (the poison row from <before>) and this customer not yet in it, the
             condition must fail closed here - proving there is no live-scan fallback masking
             the precomputed-table read. -->
        <actionGroup ref="LoginToStorefrontActionGroup" stepKey="loginAsCustomer">
            <argument name="Customer" value="$$createCustomer$$"/>
        </actionGroup>
        <amOnPage url="$createProduct.custom_attributes[url_key]$.html" stepKey="goToProductPage1"/>
        <waitForPageLoad stepKey="waitForProductPage1"/>
        <actionGroup ref="AddToCartFromStorefrontProductPageActionGroup" stepKey="addProductToCart1">
            <argument name="productName" value="$createProduct.name$"/>
        </actionGroup>
        <amOnPage url="checkout/cart" stepKey="goToCart1"/>
        <waitForPageLoad stepKey="waitForCartPage1"/>
        <actionGroup ref="PlaceOrderWithLoggedUserActionGroup" stepKey="placeFirstOrder"/>
        <magentoCLI command="queue:consumers:start ordo.automation.campaign.dispatch --max-messages=50" stepKey="drainDispatchQueueAfterFirstOrder"/>

        <!-- Step 3: force RecomputeRfmScores to run now (real schedule 01:00 daily, see
             etc/crontab.xml, which a CI run can't wait out) - this both replaces the poison row
             and adds a real row for our customer (recomputeAndStoreScores() deletes the whole
             table and reinserts fresh). -->
        <helper class="\Ordo\Automation\Test\Mftf\Helper\CronScheduleHelper" method="scheduleJobNow" stepKey="scheduleRecomputeRfmScoresJob">
            <argument name="jobCode">ordo_automation_recompute_rfm_scores</argument>
        </helper>
        <magentoCLI command="cron:run --group=default" stepKey="runRecomputeRfmScoresCron"/>

        <!-- Step 4: a SECOND real order - the customer is now in the freshly-recomputed table,
             so the condition (percentile >= 0, trivially true for anyone present) is satisfied
             this time. -->
        <amOnPage url="$createProduct.custom_attributes[url_key]$.html" stepKey="goToProductPage2"/>
        <waitForPageLoad stepKey="waitForProductPage2"/>
        <actionGroup ref="AddToCartFromStorefrontProductPageActionGroup" stepKey="addProductToCart2">
            <argument name="productName" value="$createProduct.name$"/>
        </actionGroup>
        <amOnPage url="checkout/cart" stepKey="goToCart2"/>
        <waitForPageLoad stepKey="waitForCartPage2"/>
        <actionGroup ref="PlaceOrderWithLoggedUserActionGroup" stepKey="placeSecondOrder"/>

        <!-- A brief settle wait before draining — defensive against a message-visibility race
             under CI load: sales_order_place_after's own publish() commits asynchronously
             relative to the checkout redirect this action group waits for, so a consumer that
             starts draining before that INSERT is visible would find an empty queue and exit
             (consumers-wait-for-messages=0) having dispatched nothing, silently leaving the
             order-confirmation email as the "latest" one instead of this campaign's. Same
             reasoning/fix as AdminCampaignMultipleConditionsAndTest.xml's identical step. -->
        <wait time="2" stepKey="waitForOrder2DispatchMessageVisible"/>
        <magentoCLI command="queue:consumers:start ordo.automation.campaign.dispatch --max-messages=50" stepKey="drainDispatchQueueAfterSecondOrder"/>

        <!-- Step 5: the real, merchant-visible output only a genuinely-precomputed,
             genuinely-found percentile rank could have produced. -->
        <helper class="\Ordo\Automation\Test\Mftf\Helper\MailHogHelper" method="seeTextInLatestEmail" stepKey="seePercentileConditionCopyInEmail">
            <argument name="expectedText">Recency percentile condition fired.</argument>
            <argument name="toAddress">$createCustomer.email$</argument>
        </helper>
    </test>
</tests>
